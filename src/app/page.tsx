import StudentsTable from "@/components/Table";

const page = () => {
  return (
    <div className="w-screen h-screen flex justify-center">
      <StudentsTable />
    </div>
  );
};

export default page;
